// import './env/client';
// import './env/server';

/** @type {import('next').NextConfig} */
const config = {
  reactStrictMode: true,
  images: {
    domains: ['cdn.sanity.io'],
  },
};

export default config;
